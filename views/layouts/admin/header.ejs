<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Pixel-Point</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/css/preloader.css" />

    <link
      href="https://fonts.googleapis.com/css2?family=Nunito+Sans:wght@300;400;600;700;800;900&display=swap"
      rel="stylesheet"
    />

    <link
      href="https://fonts.googleapis.com/css2?family=Nunito+Sans:wght@300;400;600;700;800;900&display=swap"
      rel="stylesheet"
    />
    <link
      rel="stylesheet"
      href="https://cdn-uicons.flaticon.com/2.0.0/uicons-regular-straight/css/uicons-regular-straight.css"
    />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.7.2/css/all.min.css"
      integrity="sha512-Evv84Mr4kqVGRNSgIGL/F/aIDqQb7xQ2vcrdIwxfjThSH8CSR7PBEakCr51Ck+w+/U6swU2Im1vVX0SVk9ABhg=="
      crossorigin="anonymous"
      referrerpolicy="no-referrer"
    />

  </head>

  <body>

    <!-- Top Navigation Bar -->
<nav class="navbar fixed-top d-lg-none bg-primary">
  <div class="container-fluid">
    <button class="navbar-toggler border-0" type="button" id="sidebarToggle">
      <i class="fas fa-bars text-white"></i>
    </button>
    <span class="navbar-brand text-white">Pixel-Point</span>
  </div>
</nav>

<div class="sidebar" id="sidebar">
  <div class="d-flex justify-content-between align-items-center mb-4">
    <h2 class="text-white mb-0 d-none d-lg-block">Pixel-Point</h2>
    <button
      class="btn-close d-lg-none text-white"
      id="sidebarClose"
    ></button>
  </div>
  <a href="/admin"
  class="<%= currentPage === 'dashboard' ? 'active' : '' %>"
    ><i class="fas fa-chart-line me-2 " ></i>Dashboard</a
  >
  <a href="/admin/users"  class="<%= currentPage === 'users' ? 'active' : '' %>" ><i class="fas fa-users me-2"></i>Users</a>
  <a href="/admin/category" class="<%= currentPage === 'category' ? 'active' : '' %>" ><i class="fas fa-tags me-2"></i>Categories</a>
  <a href="/admin/brands" class="<%= currentPage === 'brands' ? 'active' : '' %>" ><i class="fas fa-building me-2"></i>Brands</a>
  <a href="/admin/addProducts"
  class="<%= currentPage === 'addProduct' ? 'active' : '' %>"
    ><i class="fas fa-plus-circle me-2"></i>Add Products</a
  >
  <a href="/admin/products" class="<%= currentPage === 'products' ? 'active' : '' %>"><i class="fas fa-box me-2"></i>Products</a>
  <a href="/admin/coupons"class="<%= currentPage === 'coupons' ? 'active' : '' %>"><i class="fa-solid fa-tag"></i>Coupons</a>
  <a href="/admin/orders"class="<%= currentPage === 'orders' ? 'active' : '' %>"><i class="fa-solid fa-book me-2"></i>Orders</a>
  <a href="#"><i class="fas fa-cog me-2"></i>Settings</a>
  <a href="/admin/logout"><i class="fas fa-sign-out-alt me-2"></i>Logout</a>
</div>

    <div id="preloader" class="preloader">
      <div class="preloader-logo"></div>
      <div class="loading-spinner"></div>
    </div>
    <script src="/scripts/preloader.js"></script>

